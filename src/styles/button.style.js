import { styled, Button } from '@mui/material'
import AddIcon from '@mui/icons-material/Add';

export const StyledIconButton = styled(Button)(props => ({
    borderRadius: 50,
}))
  
export const StyledAddIcon = styled(AddIcon)(props => ({
    borderRadius: 50,
}))